#!/bin/bash
cd "/insert/here/the/path/to/the/python_script" # Changes the current working directory of the shell to the specified absolute path.
python3 livehtml.py "/insert/here/the/path/to/the/website/index.html" # Executes the Python 3 interpreter passing the script and the positional argument.
